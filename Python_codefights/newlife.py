# -*- coding: utf-8 -*-
"""
Created on Tue Jul 18 20:17:10 2017

@author: mthoren
"""
seed=[[0,1,0,0], 
      [1,0,1,0], 
      [0,1,0,0]]
cols = len(seed[0])
rows = len(seed)

def nextGameOfLife(seed):
    nextseed = seed [:]
    cols = len(seed[0])
    rows = len(seed)
    for col in range (cols):
        for row in range (rows):
            True
        
    return [0]    